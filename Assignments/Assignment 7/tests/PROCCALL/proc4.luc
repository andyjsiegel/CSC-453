PROGRAM P;

VAR x : INTEGER;

PROCEDURE Q (
   a : INTEGER;
   b : INTEGER;
   c : INTEGER;
   d : INTEGER;
   e : INTEGER;
   f : INTEGER;
   g : INTEGER;
   h : INTEGER);
BEGIN
   x := a + b + c + d + e + f + g + h;
END;

BEGIN
   x := 23; 
   Q(1,2,3,4,5,6,7,8);
   WRITE x - 36; WRITELN;
END.
